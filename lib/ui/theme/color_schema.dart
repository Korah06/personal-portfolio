import 'package:flutter/material.dart';
const ColorScheme themeColorSchema = ColorScheme(
  primary: Color(0xFF9290C3), // Used for primary elements
  primaryContainer: Color(0xFF1B1A55), // Used for primary container elements
  secondary: Color(0xFF535C91), // Used for secondary elements
  secondaryContainer: Color(0xFF9290C3), // Used for secondary container elements
  surface: Color(0xFF535C91), // Used for background color
  error: Color(0xFFE76F51), // Used for error elements
  onPrimary: Colors.white, // Used for text/icons on primary elements
  onSecondary: Colors.white, // Used for text/icons on secondary elements
  onSurface: Colors.white, // Used for text/icons on background
  onError: Colors.white, // Used for text/icons on error elements
  brightness: Brightness.light, // Defines the overall brightness of the theme
);
