import 'package:flutter/material.dart';
const ColorScheme themeColorSchema = ColorScheme(
  primary: Color(0xFFe7cabd), // Used for primary elements
  primaryContainer: Color(0xFF494848), // Used for primary container elements
  secondary: Color(0xFFbd6c62), // Used for secondary elements
  secondaryContainer: Color(0xFFe65d57), // Used for secondary container elements
  surface: Color(0xFFf4e9dc), // Used for background color
  error: Color(0xFFE76F51), // Used for error elements
  onPrimary: Color(0xFFa3a49e), // Used for text/icons on primary elements
  onSecondary: Color(0xFF001219), // Used for text/icons on secondary elements
  onSurface: Color(0xFF938681), // Used for text/icons on background
  onError: Colors.white, // Used for text/icons on error elements
  brightness: Brightness.light, // Defines the overall brightness of the theme
);

//Crema
// #f4e9dc
// #e7cabd

//Rojo
// #e65d57

//Negro
// #494848